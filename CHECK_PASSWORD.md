# 🔍 Проверка пароля PostgreSQL

## ✅ PostgreSQL запущен и работает!

Служба `postgresql-16` работает, порт 5432 доступен.

## 🔑 Проверка пароля

### Способ 1: Через pgAdmin (самый простой)

1. Откройте **pgAdmin 4**
2. Попробуйте подключиться к серверу с паролем `admin123`
3. Если не работает - пароль другой

### Способ 2: Попробуйте другие распространенные пароли

Часто используют:
- `admin123`
- `postgres`
- `root`
- `password`
- `123456`
- Пустой пароль (если не устанавливали)

### Способ 3: Сброс пароля через pgAdmin

1. Откройте pgAdmin
2. Если можете подключиться (даже с другим паролем):
   - Правый клик на пользователе `postgres` → **Properties**
   - Вкладка **Definition** → измените пароль на `admin123`
   - Сохраните

### Способ 4: Сброс через файл pg_hba.conf

#### Шаг 1: Найдите файл pg_hba.conf

Обычно в:
```
C:\Program Files\PostgreSQL\16\data\pg_hba.conf
```

#### Шаг 2: Сделайте резервную копию

```powershell
Copy-Item "C:\Program Files\PostgreSQL\16\data\pg_hba.conf" "C:\Program Files\PostgreSQL\16\data\pg_hba.conf.backup"
```

#### Шаг 3: Откройте файл (нужны права администратора)

```powershell
notepad "C:\Program Files\PostgreSQL\16\data\pg_hba.conf"
```

#### Шаг 4: Найдите строку для localhost

Найдите:
```
host    all             all             127.0.0.1/32            scram-sha-256
```

Временно замените на:
```
host    all             all             127.0.0.1/32            trust
```

**⚠️ ВАЖНО:** Это отключает проверку пароля! Используйте только для сброса.

#### Шаг 5: Сохраните файл и перезапустите PostgreSQL

```powershell
Restart-Service postgresql-16
```

#### Шаг 6: Подключитесь без пароля

Найдите psql.exe:
```
C:\Program Files\PostgreSQL\16\bin\psql.exe
```

Или используйте pgAdmin для подключения без пароля.

#### Шаг 7: Измените пароль

В psql или pgAdmin выполните:
```sql
ALTER USER postgres WITH PASSWORD 'admin123';
```

#### Шаг 8: Верните pg_hba.conf обратно

Верните `scram-sha-256` и перезапустите:
```powershell
Restart-Service postgresql-16
```

## 🎯 Быстрое решение

### Если у вас есть доступ к pgAdmin:

1. Откройте pgAdmin
2. Подключитесь к серверу (попробуйте разные пароли)
3. Измените пароль на `admin123`
4. Обновите `.env`:
   ```env
   DATABASE_URL="postgresql://postgres:admin123@localhost:5432/personal227info?schema=public"
   ```
5. Попробуйте: `npm run db:push`

## 📝 После сброса пароля

Убедитесь, что в `.env` правильная строка:
```env
DATABASE_URL="postgresql://postgres:admin123@localhost:5432/personal227info?schema=public"
```

И что база данных `personal227info` создана!



