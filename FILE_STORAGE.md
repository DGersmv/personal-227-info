# 📁 Структура хранения файлов

## ⚠️ ВАЖНО: Безопасное хранение файлов

**Все файлы хранятся ВНЕ папки `public/` для защиты от прямого доступа.**

Каждый файл отдается через API с проверкой прав доступа пользователя.

---

## Текущая реализация

### 1. **Фотографии объектов**
**Путь:** `uploads/objects/[objectId]/photos/` (ВНЕ public/)

**Пример:**
```
uploads/
  objects/
    1/
      photos/
        1703123456789-abc123.jpg
        1703123456790-def456.png
```

**URL для доступа:** `/api/files/photos/[objectId]/[filename]` (через API с проверкой прав)

**Реализовано:** ✅
- Загрузка через `/api/objects/[id]/photos` (POST) - **поддержка множественной загрузки**
- Просмотр через `/api/files/photos/[objectId]/[filename]` (GET с проверкой прав)
- Файлы сохраняются с уникальными именами: `timestamp-random.ext`
- **Права на загрузку:** DESIGNER, BUILDER, CUSTOMER, ADMIN
- **Максимальный размер:** 10 MB на файл
- **Проверка прав доступа:** 
  - Заказчик видит только свои объекты и видимые фото
  - Проектировщик/Строитель - только назначенные объекты (могут загружать фото из BIM программ)
- **Автоматическое разделение:** При загрузке с телефона система автоматически разделяет фото и видео

---

### 2. **Видео объектов**
**Путь:** `uploads/objects/[objectId]/videos/` (ВНЕ public/)

**Пример:**
```
uploads/
  objects/
    1/
      videos/
        1703123456789-abc123.mp4
        1703123456790-def456.mov
```

**URL для доступа:** `/api/files/videos/[objectId]/[filename]` (через API с проверкой прав)

**Реализовано:** ✅
- Загрузка через `/api/objects/[id]/videos` (POST) - поддержка множественной загрузки
- Просмотр через `/api/files/videos/[objectId]/[filename]` (GET с проверкой прав)
- Файлы сохраняются с уникальными именами: `timestamp-random.ext`
- **Права на загрузку:** DESIGNER, BUILDER, CUSTOMER, ADMIN
- **Максимальный размер:** 500 MB
- **Проверка прав доступа:** 
  - Заказчик видит только свои объекты и видимые видео
  - Проектировщик/Строитель - только назначенные объекты (могут загружать видео из BIM программ)
- **Автоматическое разделение:** При загрузке с телефона система автоматически разделяет фото и видео

---

### 3. **Изображения портфолио**
**Путь:** `uploads/portfolio/avatars/` и `uploads/portfolio/covers/` (ВНЕ public/)

**Пример:**
```
uploads/
  portfolio/
    avatars/
      1-1703123456789.jpg
      2-1703123456790.png
    covers/
      1-1703123456789.jpg
      2-1703123456790.png
```

**URL для доступа:**
- Аватары: `/api/files/portfolio/avatars/[filename]` (через API)
- Обложки: `/api/files/portfolio/covers/[filename]` (через API)

**Реализовано:** ⚠️ Частично
- Загрузка через `/api/portfolio/upload` (POST)
- Формат имени: `userId-timestamp.ext`
- **TODO:** Создать API для отдачи файлов портфолио с проверкой прав

---

### 4. **Загрузки (Downloads)**
**Путь:** `uploads/downloads/` (ВНЕ public/)

**Пример:**
```
uploads/
  downloads/
    1764232495439-LandscapeHelper_AC27.apx
    1764232908947-LandscapeHelper_AC27.apx
```

**URL для доступа:** `/api/downloads/[id]/download` (через API с проверкой прав)

**Реализовано:** ✅
- Загрузка через `/api/downloads/upload` (POST)
- Скачивание через `/api/downloads/[id]/download` (GET с проверкой прав)
- Формат имени: `timestamp-originalName`
- **Логика доступа:**
  - **Каталог:** Все загрузки доступны всем проектировщикам для просмотра
  - **Мои загрузки:** Только купленные/скачанные конкретным проектировщиком
  - Бесплатные загрузки доступны всем проектировщикам
  - Платные загрузки требуют покупки через `/api/downloads/[id]/purchase`

---

## Что нужно реализовать

### 5. **Панорамы 360°**
**Планируемый путь:** `uploads/objects/[objectId]/panoramas/` (ВНЕ public/)

**Пример:**
```
uploads/
  objects/
    1/
      panoramas/
        1703123456789-panorama1.jpg
        1703123456790-panorama2.jpg
      panoramas/
        thumbnails/
          1703123456789-panorama1-thumb.jpg
```

**URL для доступа:** `/api/files/panoramas/[objectId]/[filename]` (через API с проверкой прав)

**Статус:** ❌ Не реализовано
- Нужно создать API: `/api/objects/[id]/panoramas` (POST, GET)
- Нужно создать API для отдачи: `/api/files/panoramas/[objectId]/[filename]` (GET с проверкой прав)
- Панорамы обычно большие файлы (10-50 MB)
- Нужны миниатюры для предпросмотра

---

### 6. **Документы (PDF, DOCX и т.д.)**
**Планируемый путь:** `uploads/objects/[objectId]/documents/` или `uploads/projects/[projectId]/documents/` (ВНЕ public/)

**Пример:**
```
uploads/
  objects/
    1/
      documents/
        1703123456789-contract.pdf
        1703123456790-plan.dwg
  projects/
    1/
      documents/
        1703123456789-specification.pdf
```

**URL для доступа:** 
- Объекты: `/api/files/documents/objects/[objectId]/[filename]` (через API с проверкой прав и оплаты)
- Проекты: `/api/files/documents/projects/[projectId]/[filename]` (через API с проверкой прав и оплаты)

**Статус:** ❌ Не реализовано
- Нужно создать API: `/api/objects/[id]/documents` и `/api/projects/[id]/documents` (POST, GET)
- Нужно создать API для отдачи: `/api/files/documents/objects/[objectId]/[filename]` (GET с проверкой прав и оплаты)
- Документы могут быть защищены (требовать оплаты)
- **Обязательна защита от прямого доступа** - все документы через API

---

## Структура хранения (защищенная)

### **Все файлы хранятся ВНЕ `public/`:**

```
uploads/  (в корне проекта, ВНЕ public/)
  objects/
    [id]/
      photos/
      panoramas/
      documents/
  projects/
    [id]/
      documents/
  portfolio/
    avatars/
    covers/
  downloads/
```

### **Безопасность**

**✅ Реализовано:**
- Все файлы хранятся в `uploads/` (вне `public/`)
- Доступ к файлам только через API с проверкой прав
- Каждый запрос проверяет:
  - Авторизацию пользователя
  - Права доступа к объекту/проекту
  - Видимость файла (для заказчиков)
  - Статус оплаты (для документов)

**API для отдачи файлов:**
- Фото: `/api/files/photos/[objectId]/[filename]`
- Панорамы: `/api/files/panoramas/[objectId]/[filename]` (TODO)
- Документы: `/api/files/documents/objects/[objectId]/[filename]` (TODO)
- Портфолио: `/api/files/portfolio/avatars/[filename]` (TODO)

### 3. **Миниатюры**

**Текущее состояние:**
- ✅ В БД есть поля для миниатюр (`thumbnailFilename`, `thumbnailFilePath`)
- ❌ Создание миниатюр не реализовано

**Рекомендация:**
- Использовать библиотеку `sharp` для создания миниатюр
- Сохранять миниатюры рядом с оригиналами:
  ```
  photos/
    original.jpg
    thumbnails/
      original-thumb.jpg
  ```

### 4. **Ограничения размера**

**Текущие ограничения:**
- Фото: 10 MB
- Портфолио: 10 MB

**Рекомендации:**
- Панорамы: 50 MB (они большие)
- Документы: 100 MB (могут быть большие PDF/DWG)
- Видео (если будет): 500 MB

### 5. **Очистка неиспользуемых файлов**

**Рекомендация:**
- Создать скрипт для очистки файлов, которые не связаны с записями в БД
- Запускать периодически (например, раз в месяц)

---

## План реализации

### Приоритет 1: Панорамы
1. Создать API `/api/objects/[id]/panoramas`
2. Реализовать загрузку панорам
3. Создать миниатюры для панорам
4. Интеграция с OpenGlobus для просмотра

### Приоритет 2: Документы
1. Создать API `/api/objects/[id]/documents` и `/api/projects/[id]/documents`
2. Реализовать загрузку документов
3. Реализовать защищенный просмотр (с проверкой прав и оплаты)
4. PDF viewer для просмотра

### Приоритет 3: Миниатюры для фото
1. Установить `sharp`
2. Создать миниатюры при загрузке фото
3. Сохранять пути к миниатюрам в БД

---

## Примеры использования

### Загрузка фото
```typescript
const formData = new FormData();
formData.append('file', file);
formData.append('folderId', folderId);
formData.append('isVisibleToCustomer', 'true');

const response = await fetch(`/api/objects/${objectId}/photos`, {
  method: 'POST',
  body: formData,
});
```

### Просмотр фото
```typescript
// Фото доступно напрямую по URL
<img src={`/uploads/objects/${objectId}/photos/${filename}`} />
```

### Защищенный просмотр документа (будущее)
```typescript
// Документ отдается через API с проверкой прав
const response = await fetch(`/api/documents/${documentId}/view`);
const blob = await response.blob();
const url = URL.createObjectURL(blob);
```

