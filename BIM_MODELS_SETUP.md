# 🏗️ Настройка функционала 3D моделей

## ✅ Что было реализовано

1. **База данных:**
   - Добавлена модель `BimModel` в Prisma схему
   - Поддержка исходных файлов (SketchUp, Revit, ArchiCAD и др.)
   - Поддержка файлов для просмотра (IFC, glTF)
   - Комментарии к моделям

2. **API Routes:**
   - `GET /api/objects/[id]/models` - список моделей
   - `POST /api/objects/[id]/models` - загрузка модели
   - `GET /api/objects/[id]/models/[modelId]` - информация о модели
   - `DELETE /api/objects/[id]/models/[modelId]` - удаление модели
   - `GET /api/objects/[id]/models/[modelId]/download` - скачивание файлов
   - `GET /api/objects/[id]/models/[modelId]/view` - получение файла для просмотра

3. **Компоненты:**
   - `BimModelUploader` - форма загрузки моделей
   - `BimModelViewer` - просмотрщик моделей (IFC и glTF)
   - `BimModelsList` - список моделей

4. **Страницы:**
   - `/objects/[id]/models` - страница со списком моделей
   - Добавлена вкладка "3D Модели" в навигацию объекта

## 📋 Следующие шаги

### 1. Обновить базу данных

```powershell
# Сгенерировать Prisma Client с новой схемой
npm run db:generate

# Применить изменения к базе данных
npm run db:push
```

### 2. Протестировать функционал

1. Зайти на страницу объекта
2. Перейти на вкладку "3D Модели"
3. Загрузить тестовую модель:
   - Исходный файл (обязательно): .skp, .rvt, .pln и т.д.
   - Файл для просмотра (опционально): .ifc или .gltf/.glb

### 3. Улучшения (опционально)

#### Просмотрщик IFC
Для полноценного просмотра IFC файлов в браузере можно установить библиотеку:

```powershell
npm install web-ifc-viewer
```

И обновить компонент `BimModelViewer.tsx` для использования этой библиотеки.

#### Просмотрщик glTF
Для просмотра glTF уже используется Google Model Viewer через CDN (автоматически загружается).

## 🔐 Права доступа

- **Проектировщики (DESIGNER):** могут загружать, редактировать и удалять свои модели
- **Строители (BUILDER):** могут просматривать и скачивать модели
- **Заказчики (CUSTOMER):** могут просматривать и скачивать только видимые модели
- **Администраторы (ADMIN):** полный доступ

## 📝 Форматы файлов

### Исходные файлы (обязательные):
- SketchUp: `.skp`
- Revit: `.rvt`
- ArchiCAD: `.pln`, `.pla`
- IFC: `.ifc`
- glTF: `.gltf`, `.glb`
- OBJ: `.obj`
- 3D Studio: `.3ds`

### Файлы для просмотра (опциональные):
- IFC: `.ifc` - для BIM просмотра
- glTF: `.gltf`, `.glb` - для веб-просмотра

## 💡 Рекомендации

1. **Для проектировщиков:**
   - Загружайте исходный файл (для редактирования)
   - Экспортируйте IFC или glTF из вашей программы и загрузите как файл для просмотра
   - Это позволит другим участникам просматривать модель в браузере

2. **Размеры файлов:**
   - Исходный файл: до 500 MB
   - Файл для просмотра: до 200 MB

3. **Версионирование:**
   - Каждая загрузка создает новую модель
   - Используйте поле "Версия" для отслеживания версий

## 🐛 Известные ограничения

- Просмотр IFC файлов требует дополнительной настройки (библиотека web-ifc-viewer)
- Просмотр glTF работает через Google Model Viewer (CDN)
- Большие модели могут загружаться медленно

## 📚 Дополнительная информация

Для интеграции полноценного IFC просмотрщика см. документацию:
- https://github.com/IFCjs/web-ifc-viewer
- https://ifcjs.github.io/info/

