# Настройка интеграции с insales.ru

## 📋 Предварительные требования

1. Аккаунт в insales.ru
2. Тарифный план, поддерживающий API доступ
3. Доступ к настройкам приложения в админ-панели insales

## 🔧 Шаг 1: Создание OAuth приложения в insales

1. Войдите в админ-панель вашего магазина insales
2. Перейдите в раздел **"Расширения" → "Разработчикам"** (или **"Приложения" → "API"**)
3. Создайте новое OAuth приложение:
   - Название: `Personal227Info` (или любое другое)
   - Redirect URI: `https://yourdomain.com/api/auth/insales/callback`
     - ⚠️ **Важно:** URI должен точно совпадать с настройками в вашем `.env`
   - Разрешения: выберите необходимые права доступа
4. Сохраните приложение и получите:
   - **API Key** (Client ID)
   - **API Secret** (Client Secret)
   - **Shop Domain** (домен вашего магазина, например: `myshop.insales.ru`)

## 🔑 Шаг 2: Настройка переменных окружения

Добавьте в ваш `.env` файл:

```env
# Insales OAuth настройки
INSALES_API_KEY="your-api-key-here"
INSALES_API_SECRET="your-api-secret-here"
INSALES_SHOP_DOMAIN="myshop.insales.ru"
INSALES_OAUTH_REDIRECT_URI="https://yourdomain.com/api/auth/insales/callback"
```

### ⚠️ Важные замечания:

- **INSALES_SHOP_DOMAIN** - только домен, без `https://` (например: `myshop.insales.ru`)
- **INSALES_OAUTH_REDIRECT_URI** - должен точно совпадать с Redirect URI в настройках приложения insales
- Для локальной разработки можно использовать `http://localhost:3000/api/auth/insales/callback`
- В продакшене обязательно используйте HTTPS

## 🧪 Шаг 3: Тестирование

1. Убедитесь, что все переменные окружения установлены
2. Запустите проект: `npm run dev`
3. Перейдите на страницу `/login`
4. Нажмите "Войти через insales"
5. Должен произойти редирект на страницу авторизации insales
6. После авторизации вы будете перенаправлены обратно на сайт

## 🔍 Проверка работы

После успешной авторизации:

1. Проверьте, что пользователь создан в базе данных
2. Проверьте таблицу `insales_accounts` - должна быть запись о связи аккаунта
3. Проверьте, что JWT токен установлен в cookies
4. Попробуйте зайти на защищенные страницы (например, `/dashboard`)

## 🐛 Решение проблем

### Ошибка: "Insales не настроен"

- Проверьте, что все переменные окружения установлены
- Убедитесь, что переменные загружаются (перезапустите сервер)

### Ошибка: "invalid_state"

- Это ошибка CSRF защиты
- Убедитесь, что cookies работают корректно
- Проверьте настройки `sameSite` и `secure` для cookies

### Ошибка: "token_exchange_failed"

- Проверьте правильность API Key и API Secret
- Убедитесь, что Redirect URI совпадает в настройках insales и в `.env`
- Проверьте, что приложение в insales активно

### Ошибка: "user_data_failed"

- Проверьте права доступа приложения в insales
- Убедитесь, что API endpoint для получения данных пользователя доступен
- Проверьте логи сервера для деталей ошибки

## 📚 Дополнительная информация

### API Endpoints insales

- Авторизация: `https://{shop}.insales.ru/admin/applications/authorize`
- Обмен токена: `https://{shop}.insales.ru/admin/oauth/token`
- Данные аккаунта: `https://{shop}.insales.ru/admin/account.json` или `/admin/current_account.json`

### Безопасность

- ⚠️ **Никогда не коммитьте** `.env` файл с реальными ключами
- Используйте разные ключи для разработки и продакшена
- Регулярно обновляйте API Secret
- Используйте HTTPS в продакшене

## 🔄 Обновление токенов

Токены доступа insales могут истекать. В будущем можно добавить:

- Автоматическое обновление токенов через refresh_token
- Обработку истечения токенов
- Уведомления пользователям о необходимости повторной авторизации

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте документацию insales API
2. Обратитесь в поддержку insales
3. Проверьте логи приложения для деталей ошибок



