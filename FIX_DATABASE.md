# 🔧 Исправление ошибки подключения к PostgreSQL

## Ошибка: P1000 Authentication failed

Эта ошибка означает, что **неверные учетные данные** в `DATABASE_URL`.

## ✅ Решение:

### 1. Проверьте формат DATABASE_URL

Правильный формат:
```
postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE?schema=public
```

**Примеры:**

#### Для стандартной установки PostgreSQL:
```env
DATABASE_URL="postgresql://postgres:your_password@localhost:5432/personal227info?schema=public"
```

#### Если используете другого пользователя:
```env
DATABASE_URL="postgresql://myuser:mypassword@localhost:5432/personal227info?schema=public"
```

#### Если PostgreSQL на другом хосте:
```env
DATABASE_URL="postgresql://postgres:password@192.168.1.100:5432/personal227info?schema=public"
```

### 2. Проверьте учетные данные PostgreSQL

#### Вариант A: Используйте стандартного пользователя `postgres`

1. Найдите пароль, который вы установили при установке PostgreSQL
2. Используйте его в DATABASE_URL:
   ```env
   DATABASE_URL="postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/personal227info?schema=public"
   ```

#### Вариант B: Создайте нового пользователя

1. Подключитесь к PostgreSQL (через pgAdmin или psql)
2. Создайте пользователя:
   ```sql
   CREATE USER myuser WITH PASSWORD 'mypassword';
   GRANT ALL PRIVILEGES ON DATABASE personal227info TO myuser;
   ```
3. Используйте в DATABASE_URL:
   ```env
   DATABASE_URL="postgresql://myuser:mypassword@localhost:5432/personal227info?schema=public"
   ```

### 3. Проверьте, что PostgreSQL запущен

#### Windows:
```powershell
# Проверить службу PostgreSQL
Get-Service -Name postgresql*

# Если не запущена, запустить:
Start-Service -Name postgresql-x64-14  # замените на вашу версию
```

#### Или через Services:
1. Нажмите `Win + R`
2. Введите `services.msc`
3. Найдите службу PostgreSQL
4. Убедитесь, что она запущена

### 4. Проверьте, что база данных создана

Подключитесь к PostgreSQL и создайте базу:

```sql
-- Подключитесь к PostgreSQL (обычно через пользователя postgres)
-- Создайте базу данных
CREATE DATABASE personal227info;

-- Если база уже существует, можно проверить:
\l  -- список баз данных
```

### 5. Проверьте порт PostgreSQL

По умолчанию PostgreSQL использует порт **5432**.

Если используете другой порт:
```env
DATABASE_URL="postgresql://postgres:password@localhost:5433/personal227info?schema=public"
```

## 🔍 Диагностика:

### Проверка подключения через psql:

```powershell
# Попробуйте подключиться напрямую
psql -U postgres -h localhost -d personal227info

# Или с указанием пароля
psql -U postgres -h localhost -d personal227info -W
```

Если подключение работает через psql, но не работает через Prisma - проблема в формате DATABASE_URL.

## ⚠️ Частые ошибки:

### 1. Специальные символы в пароле

Если в пароле есть специальные символы (`@`, `#`, `%`, `&` и т.д.), их нужно **URL-кодировать**:

```env
# Пароль: my@pass#123
# Правильно:
DATABASE_URL="postgresql://postgres:my%40pass%23123@localhost:5432/personal227info?schema=public"
```

**Символы для кодирования:**
- `@` → `%40`
- `#` → `%23`
- `%` → `%25`
- `&` → `%26`
- `:` → `%3A`
- `/` → `%2F`
- `?` → `%3F`
- `=` → `%3D`
- ` ` (пробел) → `%20`

### 2. Пробелы в строке

Убедитесь, что нет пробелов вокруг `=`:
```env
# ❌ НЕПРАВИЛЬНО:
DATABASE_URL = "postgresql://..."

# ✅ ПРАВИЛЬНО:
DATABASE_URL="postgresql://..."
```

### 3. Кавычки

Используйте **двойные кавычки**:
```env
# ✅ ПРАВИЛЬНО:
DATABASE_URL="postgresql://..."

# ❌ НЕПРАВИЛЬНО:
DATABASE_URL='postgresql://...'
```

## 🧪 Тест подключения:

После исправления DATABASE_URL попробуйте:

```bash
npm run db:push
```

Если ошибка сохраняется, проверьте:
1. ✅ PostgreSQL запущен
2. ✅ База данных `personal227info` создана
3. ✅ Логин и пароль правильные
4. ✅ Порт правильный (обычно 5432)
5. ✅ Специальные символы в пароле закодированы



