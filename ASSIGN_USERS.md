# 👥 Назначение пользователей на объекты

## Зачем это нужно?

Для того, чтобы **Проектировщик** и **Строитель** могли видеть и работать с объектами, их нужно **назначить** на эти объекты.

## Автоматическое назначение

### Скрипт для назначения на первый объект:

```bash
node assign-users-to-object.js
```

Этот скрипт:
- Найдет первый объект в базе
- Назначит Проектировщика (`designer@test.com`) на объект
- Назначит Строителя (`builder@test.com`) на объект

## Ручное назначение через Prisma Studio

1. Запустите Prisma Studio:
   ```bash
   npm run db:studio
   ```

2. Откройте `http://localhost:5555`

3. Перейдите в таблицу `user_object_assignments`

4. Создайте запись:
   - `userId` - ID пользователя (Проектировщика или Строителя)
   - `objectId` - ID объекта
   - `role` - Роль для этого объекта (`DESIGNER` или `BUILDER`)

## Назначение через API (в будущем)

В будущем можно будет назначить пользователей через админ-панель или интерфейс объекта.

## Проверка назначения

После назначения:
- **Проектировщик** сможет видеть объект в списке и создавать проекты
- **Строитель** сможет видеть объект в списке и загружать фото/панорамы

## Важно

- Один пользователь может быть назначен на несколько объектов
- На одном объекте может быть несколько Проектировщиков и Строителей
- Заказчик автоматически является владельцем своих объектов (не требует назначения)



