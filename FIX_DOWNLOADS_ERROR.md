# 🔧 Исправление ошибки 500 в /api/downloads

## Проблема
Ошибка 500 при загрузке списка загрузок.

## Возможные причины и решения

### 1. Prisma Client не обновлен

**Решение:**
1. Остановите сервер (Ctrl+C)
2. Запустите:
   ```bash
   npm run db:generate
   ```
3. Запустите сервер снова:
   ```bash
   npm run dev
   ```

### 2. Таблица не существует в БД

**Решение:**
1. Проверьте, что схема применена:
   ```bash
   npm run db:push
   ```

### 3. Проблема с enum

**Решение:**
Код уже обновлен для работы со строками. Если проблема сохраняется, проверьте логи сервера.

## Проверка логов

Откройте консоль, где запущен `npm run dev`, и найдите строку:
```
Ошибка получения загрузок: ...
```

Это покажет точную причину ошибки.

## Быстрое исправление

Если ничего не помогает:

1. Остановите сервер
2. Удалите `.prisma` папку в `node_modules` (если есть)
3. Запустите:
   ```bash
   npm run db:generate
   npm run db:push
   ```
4. Запустите сервер снова

## Альтернатива

Если проблема с enum, можно временно убрать фильтр по статусу:

В `app/api/downloads/route.ts` замените:
```typescript
const where: any = {
  status: 'ACTIVE' as any,
};
```

На:
```typescript
const where: any = {}; // Без фильтра по статусу
```

Это покажет все загрузки, включая неактивные.



