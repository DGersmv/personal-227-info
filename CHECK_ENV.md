# ✅ Проверка файла .env

## Обязательные переменные:

### 1. DATABASE_URL ✅
```env
DATABASE_URL="postgresql://user:password@localhost:5432/personal227info?schema=public"
```
**Проверьте:**
- ✅ Заменили `user` на ваше имя пользователя PostgreSQL
- ✅ Заменили `password` на ваш пароль PostgreSQL
- ✅ Заменили `localhost:5432` если PostgreSQL на другом хосте/порту
- ✅ База данных `personal227info` создана

### 2. JWT_SECRET ✅
```env
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
```
**Проверьте:**
- ✅ Заменили на случайную строку (минимум 32 символа)
- ✅ Не используете значение по умолчанию в продакшене

**Сгенерировать можно:**
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### 3. NODE_ENV ✅
```env
NODE_ENV="development"
```
**Проверьте:**
- ✅ Для разработки: `development`
- ✅ Для продакшена: `production`

### 4. INSALES переменные (можно оставить пустыми для начала) ⚠️

```env
INSALES_API_KEY=""
INSALES_API_SECRET=""
INSALES_SHOP_DOMAIN=""
INSALES_OAUTH_REDIRECT_URI="http://localhost:3000/api/auth/insales/callback"
```

**Проверьте:**
- ⚠️ Если планируете использовать авторизацию через insales - заполните эти поля
- ⚠️ Если пока не используете insales - можно оставить пустыми
- ⚠️ `INSALES_OAUTH_REDIRECT_URI` должен совпадать с Redirect URI в настройках OAuth приложения

## 📋 Чек-лист:

- [ ] DATABASE_URL настроен с правильными данными PostgreSQL
- [ ] JWT_SECRET заменен на случайную строку (не дефолтное значение)
- [ ] NODE_ENV установлен в `development` для разработки
- [ ] INSALES переменные заполнены (если используете insales) или оставлены пустыми

## 🚀 После настройки:

1. Примените схему базы данных:
   ```bash
   npm run db:push
   ```

2. Перезапустите dev сервер:
   ```bash
   npm run dev
   ```

## ❌ Частые ошибки:

### "Can't reach database server"
- Проверьте, что PostgreSQL запущен
- Проверьте DATABASE_URL (правильный хост, порт, логин, пароль)

### "JWT_SECRET is too short"
- Используйте минимум 32 символа
- Сгенерируйте новую случайную строку

### "Insales не настроен"
- Это нормально, если вы не используете insales пока
- Или заполните INSALES переменные



