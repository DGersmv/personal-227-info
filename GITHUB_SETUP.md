# 📤 Выгрузка проекта на GitHub

## Шаг 1: Проверка и инициализация Git

```bash
# Проверить статус Git
git status

# Если репозиторий еще не инициализирован:
git init

# Настроить имя и email (если еще не настроено)
git config user.name "Ваше Имя"
git config user.email "ваш@email.com"
```

## Шаг 2: Добавление файлов и первый коммит

```bash
# Добавить все файлы (кроме тех, что в .gitignore)
git add .

# Проверить, что будет добавлено
git status

# Создать первый коммит
git commit -m "Initial commit: проект управления объектами с фото, видео, комментариями и папками"
```

## Шаг 3: Создание репозитория на GitHub

1. Зайдите на https://github.com
2. Нажмите кнопку **"New repository"** (или **"+"** → **"New repository"**)
3. Заполните:
   - **Repository name:** `personal227info` (или другое имя)
   - **Description:** "Платформа управления объектами строительства"
   - **Visibility:** Private (рекомендуется) или Public
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (у нас уже все есть)
4. Нажмите **"Create repository"**

## Шаг 4: Подключение к GitHub и отправка

```bash
# Добавить удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/personal227info.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/personal227info.git

# Переименовать ветку в main (если нужно)
git branch -M main

# Отправить код на GitHub
git push -u origin main
```

Если GitHub попросит авторизацию:
- Используйте **Personal Access Token** (не пароль)
- Создать токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
- Права: `repo` (полный доступ к репозиториям)

## Шаг 5: Продолжение работы дома

### Клонирование репозитория на другом компьютере:

```bash
# Клонировать репозиторий
git clone https://github.com/YOUR_USERNAME/personal227info.git

# Перейти в папку проекта
cd personal227info

# Установить зависимости
npm install

# Настроить переменные окружения
# Скопировать .env.example в .env (если есть) или создать .env вручную
# Добавить DATABASE_URL, JWT_SECRET и другие переменные

# Применить миграции Prisma
npx prisma generate
npx prisma db push

# Запустить dev сервер
npm run dev
```

### Работа с несколькими компьютерами:

**На работе (перед уходом):**
```bash
# Сохранить изменения
git add .
git commit -m "Описание изменений"
git push
```

**Дома (начало работы):**
```bash
# Получить последние изменения
git pull

# Установить зависимости (если добавились новые)
npm install

# Применить миграции (если изменилась схема Prisma)
npx prisma generate
npx prisma db push

# Запустить dev сервер
npm run dev
```

**Дома (перед уходом):**
```bash
# Сохранить изменения
git add .
git commit -m "Описание изменений"
git push
```

**На работе (начало работы):**
```bash
# Получить последние изменения
git pull
```

## ⚠️ Важные файлы, которые НЕ должны попасть в Git:

Убедитесь, что `.gitignore` содержит:
- `.env` - переменные окружения
- `node_modules/` - зависимости
- `.next/` - сборка Next.js
- `uploads/` - загруженные файлы
- `prisma/dev.db` - локальная БД (если используется SQLite)

## 📝 Полезные команды Git:

```bash
# Посмотреть историю коммитов
git log

# Посмотреть изменения
git diff

# Отменить изменения в файле (до git add)
git checkout -- имя_файла

# Отменить последний коммит (но сохранить изменения)
git reset --soft HEAD~1

# Посмотреть удаленные репозитории
git remote -v

# Изменить URL удаленного репозитория
git remote set-url origin НОВЫЙ_URL
```

## 🔐 Безопасность:

**НЕ коммитьте:**
- `.env` файлы с реальными ключами
- Пароли и токены
- Персональные данные

**Создайте `.env.example`** с примером переменных (без реальных значений):
```env
DATABASE_URL="postgresql://user:password@localhost:5432/dbname"
JWT_SECRET="your-secret-key-here"
```

