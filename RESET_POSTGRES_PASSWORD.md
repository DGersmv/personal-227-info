# 🔑 Сброс пароля PostgreSQL

## Вариант 1: Проверка текущего пароля

### Попробуйте подключиться напрямую:

```powershell
# Попробуйте подключиться с паролем admin123
psql -U postgres -h localhost -d postgres
```

Если запрашивает пароль - введите `admin123`. Если не работает - пароль другой.

## Вариант 2: Сброс пароля через pgAdmin

1. Откройте **pgAdmin**
2. Подключитесь к серверу (если помните пароль)
3. Правый клик на пользователе `postgres` → **Properties**
4. Вкладка **Definition** → измените пароль
5. Сохраните

## Вариант 3: Сброс пароля через файл pg_hba.conf

### Шаг 1: Найдите файл pg_hba.conf

Обычно находится в:
```
C:\Program Files\PostgreSQL\{версия}\data\pg_hba.conf
```

Или через поиск:
```powershell
Get-ChildItem -Path "C:\Program Files\PostgreSQL" -Recurse -Filter "pg_hba.conf" -ErrorAction SilentlyContinue
```

### Шаг 2: Измените метод аутентификации

Найдите строку:
```
host    all             all             127.0.0.1/32            scram-sha-256
```

Временно замените на:
```
host    all             all             127.0.0.1/32            trust
```

**⚠️ ВАЖНО:** Это отключает проверку пароля! Используйте только для сброса.

### Шаг 3: Перезапустите PostgreSQL

```powershell
# Найдите службу PostgreSQL
Get-Service | Where-Object {$_.Name -like "*postgres*"}

# Перезапустите (замените на имя вашей службы)
Restart-Service postgresql-x64-14
```

### Шаг 4: Подключитесь без пароля и измените пароль

```powershell
psql -U postgres -h localhost
```

В psql выполните:
```sql
ALTER USER postgres WITH PASSWORD 'admin123';
\q
```

### Шаг 5: Верните обратно pg_hba.conf

Верните `scram-sha-256` обратно и перезапустите PostgreSQL.

## Вариант 4: Создание нового пользователя (если не помните пароль postgres)

### Шаг 1: Временно измените pg_hba.conf на `trust` (см. Вариант 3)

### Шаг 2: Подключитесь без пароля

```powershell
psql -U postgres -h localhost
```

### Шаг 3: Создайте нового пользователя

```sql
CREATE USER myuser WITH PASSWORD 'mypassword';
ALTER USER myuser CREATEDB;
GRANT ALL PRIVILEGES ON DATABASE personal227info TO myuser;
\q
```

### Шаг 4: Используйте нового пользователя в .env

```env
DATABASE_URL="postgresql://myuser:mypassword@localhost:5432/personal227info?schema=public"
```

## Вариант 5: Переустановка PostgreSQL (крайний случай)

Если ничего не помогает:
1. Удалите PostgreSQL
2. Переустановите с новым паролем
3. Запомните пароль!

## 🔍 Проверка подключения

После сброса пароля проверьте:

```powershell
psql -U postgres -h localhost -d personal227info
```

Если подключение работает - обновите `.env` и попробуйте:
```bash
npm run db:push
```



